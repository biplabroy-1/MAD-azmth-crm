/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://dashboard.azmth.in', // replace with your domain
    generateRobotsTxt: true, // generates robots.txt file
    sitemapSize: 500,
    changefreq: 'daily',
    priority: 0.7,
};
